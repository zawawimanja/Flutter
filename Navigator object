 manages Flutter’s screens (also called routes or pages) inside a stack

top of stack

1. Declaring and initiliaze routes

 routes: {
        '/': (context) => const SignUpScreen(),
         '/welcome': (context) => WelcomeScreen(),

      },
